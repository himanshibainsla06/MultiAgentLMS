"use client";import {useEffect,useState} from "react";import Link from "next/link";type Note={title:string;sections:{title:string;content:string;kind:string}[]};export default function NotesReader({subject,topic}:{subject:string;topic:string}){const [note,setNote]=useState<Note>();const [error,setError]=useState("");useEffect(()=>{fetch("/api/notes/generate",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({subject,topic,style:"normal"})}).then(async r=>{const d=await r.json();if(!r.ok)throw Error(d.error);setNote({title:d.note.title,sections:d.note.sections??d.note.note_sections})}).catch(e=>setError(e.message))},[subject,topic]);if(error)return <main className="shell"><p className="card">{error}</p></main>;if(!note)return <main className="shell"><p className="card">Notes prepare ho rahe hain…</p></main>;return <main className="shell"><div style={{display:"flex",justifyContent:"space-between",gap:12,alignItems:"center"}}><div><span className="tag">{subject}</span><h1>{note.title}</h1></div><Link className="btn" href={`/assessment?subject=${encodeURIComponent(subject)}&topic=${encodeURIComponent(topic)}`}>Start practice</Link></div><div className="grid" style={{marginTop:20}}>{note.sections.map((s,i)=><details className="card" key={s.title} open={i<2}><summary style={{fontWeight:700,cursor:"pointer"}}>{s.title}</summary><div style={{whiteSpace:"pre-wrap",lineHeight:1.6,marginTop:14}}>{s.content}</div></details>)}</div></main>}
