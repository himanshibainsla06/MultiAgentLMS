"use client";import {useEffect,useState} from "react";import Link from "next/link";export default function Result({params}:{params:Promise<{id:string}>}){const [result,setResult]=useState<{score:number;correct:number;total:number;weakConcepts:string[]}|null>(null);useEffect(()=>{params.then(({id})=>{const stored=sessionStorage.getItem(`result:${id}`);if(stored)setResult(JSON.parse(stored))})},[params]);if(!result)return <main className="shell"><p className="card">Result unavailable. Please submit the assessment first.</p></main>;return <main className="shell" style={{maxWidth:700}}><article className="card"><span className="tag">Assessment complete</span><h1>{result.score}%</h1><p className="muted">{result.correct} correct out of {result.total} questions.</p><h2 className="section-title">Recommended revision</h2>{result.weakConcepts.length?<ul>{result.weakConcepts.map(c=><li key={c}>{c}</li>)}</ul>:<p>Excellent work — revisit the notes once to consolidate your learning.</p>}<Link className="btn" href="/mentor">View mentor guidance</Link></article></main>}
